# Example .env file for Town Square
# retention in hours; 0 = messages live only in memory
RETENTION_HOURS=0

# default city to pre‑fill when no location
DEFAULT_CITY=konigsbrunn

# allowed CORS origins for the API
CORS_ORIGINS=http://localhost:8080