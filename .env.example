## Example environment configuration for Town Square

# Number of hours to retain messages in SQLite.
# Set to 0 to keep messages only in memory (default).
RETENTION_HOURS=0

# Comma‑separated list of allowed origins for CORS.
# Leave blank to disable CORS.
CORS_ORIGINS=http://localhost:8080